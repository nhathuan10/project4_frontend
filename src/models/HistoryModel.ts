export type HistoryModel = {
    id: number
    userEmail: string
    checkoutDate: string
    returnedDate: string
    title: string
    author: string
    description: string
    validated: number
    verified: boolean
    img: string
    bookId: number
}